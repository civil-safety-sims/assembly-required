# PROMPT ROADMAP: ASSEMBLY REQUIRED

**INSTRUCTIONS:**
1. Create the `rules.md` file in your root directory (content provided in previous steps).
2. Copy and paste these prompts strictly in order.
3. Wait for the AI to complete one task before pasting the next.

---

### PROMPT 1: Architecture & Layout (The Skeleton)
Create a new React application using Vite and Tailwind CSS.

**1. The Layout:**
Build a "Cyberpunk Dossier" style interface with 3 distinct columns:
* **Left (The Supply Cache):** A grid of draggable items.
* **Center (The Activist):** A "Paper Doll" schematic with slots for: Head, Eyes, Face, Body, Hands, Pockets, and Feet.
* **Right (The Briefing):** A panel showing current variables: "Weather" (Hot/Cold/Rain) and "Threat Level" (Vigil/Protest/Crackdown).

**2. The Vibe:**
Use a dark mode palette (Slate/Zinc). Use Lucide-React for icons. Make the drop zones look like a tactical blueprint.

**3. Deployment Prep:**
Structure the project so it is ready for Firebase Hosting. Ensure all assets use relative paths. Create a placeholder `firebase.json` configuration file for a single-page app rewrite.

---

### PROMPT 2: The Data Schema (The "Brain")
I have a file called `rules.md` in the root directory that contains Verified Safety Data. Read it carefully.

Create a TypeScript file called `src/data/gameData.ts`.

**Step 1: Define the Interface**
Create a strict `SafetyItem` interface. It must include:
* `id`, `name`, `icon` (string name for Lucide), `slot`.
* `sourceUrl`: string (This is mandatory).
* `sourceName`: string (e.g., "EFF", "NLG").
* `attributes`: An object containing flags for physics/medical interactions:
    * `isFlammable` (boolean)
    * `isBiometric` (boolean)
    * `isAbsorbent` (boolean)
    * `isSterile` (boolean)
    * `requiresPrescription` (boolean)
    * `isLoosePills` (boolean)

**Step 2: Generate the Items**
Based *strictly* on `rules.md`, generate the item list `AVAILABLE_ITEMS`.
* Create "Polyester Hoodie" (Body, `isFlammable: true`, link to Amnesty PDF).
* Create "Cotton Flannel" (Body, `isFlammable: false`).
* Create "Tampons" (Pocket, `isAbsorbent: true`, link to NLG).
* Create "Menstrual Pads" (Pocket, `isAbsorbent: false`).
* Create "Smart Phone (FaceID On)" (Hands, `isBiometric: true`, link to EFF).
* Create "Loose Meds" (Pocket, `isLoosePills: true`, link to NLG).
* Create "Water Bottle" (Hands, `isSterile: true`, link to PHR).

---

### PROMPT 3: The Logic Engine (The Simulator)
Create a logic file called `src/logic/simulationEngine.ts`.

Implement a function `runSimulation(equippedItems, weather, threatLevel)`.

**The Rules:**
1.  **Chemical Risk:** If `ThreatLevel` is High (Tear Gas risk) AND user wears `isAbsorbent` items (like Tampons/Contacts) -> Add a "Severe Warning" with the specific `sourceUrl`.
2.  **Fire Risk:** If `ThreatLevel` is High AND user wears `isFlammable` (Polyester) -> Add a "Burn Risk" warning.
3.  **Legal Risk:** If `ThreatLevel` is High AND user has `isBiometric` -> Add a "Privacy Breach" warning citing the 5th Amendment.
4.  **Medical Risk:** If user has `isLoosePills` -> Add a warning: "Police may confiscate unidentified pills."

**Output:**
Return a `SimulationResult` object containing a Score (0-100) and an array of `FeedbackCards`. Each card must contain the `message` and the `sourceUrl`.

---

### PROMPT 4: The UI Feedback (Report Card)
Create a component called `ReportCard.tsx`.

It should display after the user clicks "Simulate."
* If the Score is < 50, show a "DANGER" header in Red.
* Render the list of `FeedbackCards` generated by the engine.
* **CRITICAL:** Each card must have a button labeled "Verify Source ([Source Name])".
* This button must be an `<a>` tag that links to the `sourceUrl` provided in the item data. Open in a new tab.

---

### PROMPT 5: Mobile Polish
I need to ensure this application is mobile-friendly.

**1. Touch Support:**
Verify that the drag-and-drop library we are using supports touch events. If not, please switch to `dnd-kit` or ensure the standard HTML5 drag-and-drop has touch polyfills.

**2. Responsive Layout:**
Update the 3-column layout for mobile screens:
* On screens smaller than 768px (tablets/phones), stack the columns vertically.
* **Order:** Top = The